1. A significant difference is that the Inheritance design has fields associated with each class. A significant similarity between the initial design and the inheritance design is that all the classes are connected to assess in one way or another.

2. A significant difference between the Interface diagram and the Inheritance diagram is that there is no property class or security class in the Interface design. A main similarity between the two designs is the methods are almost the same.
An advantage of the Inheritance design is that all assets inherit the “name” field, and assets with debts can be added by call the addAsset method. 
An advantage of the Interface design is that classes can implement both debt and asset.

4. Nope
